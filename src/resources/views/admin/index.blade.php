<title>Admin dashboard - Skyrim Hotel</title>
<x-adminLayout>
    {{-- alert create account --}}
    <div class="slideDown">
        @if (session('success'))
            @include('partials.flashMsgSuccess')
        @endif
    </div>
    ABC
</x-adminLayout>
